@using mtgen.ViewModels;
@{
    var set = new mtgen.ViewModels.Set()
    {
        Code = "c14",
        Name = "Commander 2014",
        CreatedDate = new DateTime(2014, 10, 30),
        CardCount = 337,
        StartProductName = "c14-commander-decks"
    };

    set.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2014,11,4),
            HtmlString = "Evening: Added full set of tokens, and included every token included with each deck."
        },
        new Update() {
            UpdateDate = new DateTime(2014,11,2),
            HtmlString = "Afternoon: Oops! I was missing the <strong>Planeswalker</strong> from each deck. Dumb. Fixed."
        }
    };

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}