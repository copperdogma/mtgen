@model Set
@{
    Model.StartProductName = "arn-boosters";

    Model.CardFiles = new List<string>() { "cardsMain.json" };

    // Defaults/Examples:
    //Model.Incomplete = false;
    //Model.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    //Model.PackFiles = new List<string>() { "packs.json" };
    Model.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2016,11,21),
            HtmlString = "Added Mountain (rarity=b) and rares (rarity=u2/r) into the booster packs. Thanks, Philip!"
        }
    };
}