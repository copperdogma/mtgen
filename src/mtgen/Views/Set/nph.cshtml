@model Set
@{
    Model.StartProductName = "nph-boosters";

    Model.PackFiles = new List<string>() { "../mbs/packs.json", "../som/packs.json", "packs.json" };

    Model.CardFiles = Model.CardFiles.Concat(new List<string>() { "../mbs/cardsMain.json", "../mbs/cardsToken.json", "../mbs/cardsOther.json" }).ToList();
    Model.CardFiles = Model.CardFiles.Concat(new List<string>() { "../som/cardsMain.json", "../som/cardsToken.json", "../som/cardsOther.json" }).ToList();

    Model.Updates.Add(
        new Update()
        {
            UpdateDate = new DateTime(2015, 6, 17),
            HtmlString = "Evening: You can now add Scars of Mirrodin and Mirrodin Besieged boosters."
        }
        );

    // Defaults/Examples:
    //Model.Incomplete = false;
    //Model.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    //Model.PackFiles = new List<string>() { "packs.json" };
    //Model.Updates = new List<Update>()
    //{
    //    new Update() {
    //        UpdateDate = new DateTime(2015,11,14),
    //        HtmlString = "Afternoon: Added Plunder the Graves deck (thanks, DoubleFried!)."
    //    }
    //};
}