@using mtgen.Models;
@{
    var set = new mtgen.Models.Set()
    {
        Code = "nph",
        Name = "New Phyrexia",
        CreatedDate = new DateTime(2015, 6, 13),
        CardCount = 175,
        StartProductName = "nph-boosters"
    };

    set.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2015,6,17),
            HtmlString = "Evening: You can now add Scars of Mirrodin and Mirrodin Besieged boosters."
        }
    };

    set.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    set.PackFiles = new List<string>() { "../mbs/packs.json", "../som/packs.json", "packs.json" };

    set.CardFiles = set.CardFiles.Concat(new List<string>() { "../mbs/cardsMain.json", "../mbs/cardsToken.json", "../mbs/cardsOther.json" }).ToList();
    set.CardFiles = set.CardFiles.Concat(new List<string>() { "../som/cardsMain.json", "../som/cardsToken.json", "../som/cardsOther.json" }).ToList();

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}