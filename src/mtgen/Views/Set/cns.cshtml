@model Set
@{
    Model.StartProductName = "cns-boosters";

    Model.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2014,8,23),
            HtmlString = "Upgraded to the new generator version."
        },
        new Update() {
            UpdateDate = new DateTime(2014, 5, 29),
            HtmlString = "Morning: The land slot now replaced with a Conspiracy on Construct card (50/50 chance), and that's the only place you get get one. Thanks, boocat!"
        }
    };

    // Defaults/Examples:
    //Model.Incomplete = false;
    //Model.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    //Model.PackFiles = new List<string>() { "packs.json" };
    //Model.Updates = new List<Update>()
    //{
    //    new Update() {
    //        UpdateDate = new DateTime(2015,11,14),
    //        HtmlString = "Afternoon: Added Plunder the Graves deck (thanks, DoubleFried!)."
    //    }
    //};
}