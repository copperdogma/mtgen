@model Set
@{
    Model.StartProductName = "jud-boosters";

    Model.PackFiles = new List<string>() { "../tor/packs.json", "../ody/packs.json", "packs.json" };

    Model.CardFiles = Model.CardFiles.Concat(new List<string>() { "../ody/cardsMain.json", "../ody/cardsToken.json", "../ody/cardsOther.json" }).ToList();
    Model.CardFiles = Model.CardFiles.Concat(new List<string>() { "../tor/cardsMain.json", "../tor/cardsToken.json", "../tor/cardsOther.json" }).ToList();

    // Defaults/Examples:
    //Model.Incomplete = false;
    //Model.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    //Model.PackFiles = new List<string>() { "packs.json" };
    //Model.Updates = new List<Update>()
    //{
    //    new Update() {
    //        UpdateDate = new DateTime(2015,11,14),
    //        HtmlString = "Afternoon: Added Plunder the Graves deck (thanks, DoubleFried!)."
    //    }
    //};
}