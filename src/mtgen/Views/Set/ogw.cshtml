@using mtgen.ViewModels;
@{
    var set = new mtgen.ViewModels.Set()
    {
        Code = "ogw",
        Name = "Oath of the Gatewatch",
        CreatedDate = new DateTime(2016, 1, 9),
        CardCount = 184,
        StartProductName = "ogw-prerelease"
    };

    set.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2016,1,11),
            HtmlString = "Evening: Lowered Expedition chance and added tokens."
        }
    };

    //set.Incomplete = true;

    set.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json",
        "../exp/cardsMain.json", "../bfz/cardsMain.json", "../bfz/cardsOther.json" };
    set.PackFiles = new List<string>() { "packs.json", "../bfz/packs.json" };

    ViewBag.Set = set;
}