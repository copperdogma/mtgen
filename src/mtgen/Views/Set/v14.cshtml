@{
    var set = new mtgen.Models.Set();
    set.Code = "v14";
    set.Name = "From The Vault: Annihilation";
    set.CreatedDate = new DateTime(2014, 8, 11);
    set.CardCount = 15;
    set.StartProductName = "v14-all-cards";
    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}