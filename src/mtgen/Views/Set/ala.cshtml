@using mtgen.ViewModels;
@{
    var set = new mtgen.ViewModels.Set()
    {
        Code = "ala",
        Name = "Shards of Alara",
        CreatedDate = new DateTime(2016, 1, 24),
        CardCount = 249,
        StartProductName = "ala-boosters"
    };

    set.Updates = new List<Update>()
    {
        //new Update() {
        //    UpdateDate = new DateTime(2014,9,2),
        //    HtmlString = "Evening: Upgraded to newest version, added all Prerelease pack-ins, Fat Packs, and guild sorting."
        //}
    };

    set.CardFiles = new List<string>() {
        "cardsMain.json", "cardsOther.json", "cardsToken.json",
        "../con_/cardsMain.json", "../con_/cardsOther.json", "../con_/cardsToken.json",
        "../arb/cardsMain.json", "../arb/cardsOther.json", "../arb/cardsToken.json"
    };
    set.PackFiles = new List<string>() { "packs.json", "../con_/packs.json", "../arb/packs.json" };

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}