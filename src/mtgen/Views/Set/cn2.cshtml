@model Set
@{
    Model.StartProductName = "cn2-boosters";

    Model.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    Model.PackFiles = new List<string>() { "packs.json" };

    // Defaults/Examples:
    //Model.Incomplete = false;
    //Model.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    //Model.PackFiles = new List<string>() { "packs.json" };
    Model.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2016,8,24),
            HtmlString = "Evening: Last slot is now Conspiracy-only. Main cards can now include draft-affecting cards."
        }
    };
}