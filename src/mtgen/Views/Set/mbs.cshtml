@using mtgen.ViewModels;
@{
    var set = new mtgen.ViewModels.Set()
    {
        Code = "mbs",
        Name = "Mirrodin Besieged",
        CreatedDate = new DateTime(2015, 6, 13),
        CardCount = 155,
        StartProductName = "mbs-boosters"
    };

    set.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2015,6,17),
            HtmlString = "Evening: You can now add Scars of Mirrodin boosters."
        }
    };

    set.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    set.PackFiles = new List<string>() { "../som/packs.json", "packs.json" };

    set.CardFiles = set.CardFiles.Concat(new List<string>() { "../som/cardsMain.json", "../som/cardsToken.json", "../som/cardsOther.json" }).ToList();

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}