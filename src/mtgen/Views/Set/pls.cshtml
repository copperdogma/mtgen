@using mtgen.ViewModels;
@{
    var set = new mtgen.ViewModels.Set()
    {
        Code = "pls",
        Name = "Planeshift",
        CreatedDate = new DateTime(2016, 2, 13),
        CardCount = 143,
        StartProductName = "pls-boosters"
    };

    set.Updates = new List<Update>()
    {
        //new Update() {
        //    UpdateDate = new DateTime(2014,9,2),
        //    HtmlString = "Evening: Upgraded to newest version, added all Prerelease pack-ins, Fat Packs, and guild sorting."
        //}
    };

    set.CardFiles = new List<string>() { "cardsMain.json", "cardsOther.json" };
    set.PackFiles = new List<string>() { "packs.json", "../inv/packs.json", "../apc/packs.json" };

    set.CardFiles = set.CardFiles.Concat(new List<string>() { "../inv/cardsMain.json", "../apc/cardsMain.json" }).ToList();

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}