@model Set
@{
    Model.StartProductName = "avr-prerelease";

    Model.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2014, 9, 19),
            HtmlString = "Upgraded to newest version, added all promos, Prerelease pack-ins, and the Fat Pack."
        },
        new Update() {
            UpdateDate = new DateTime(2012, 4, 7),
            HtmlString = "Fixed Silverblade Paladin: Was Uncommon instead of Rare."
        }
    };

    // Defaults/Examples:
    //Model.Incomplete = false;
    //Model.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
    //Model.PackFiles = new List<string>() { "packs.json" };
    //Model.Updates = new List<Update>()
    //{
    //    new Update() {
    //        UpdateDate = new DateTime(2015,11,14),
    //        HtmlString = "Afternoon: Added Plunder the Graves deck (thanks, DoubleFried!)."
    //    }
    //};
}