@using mtgen.ViewModels;
@{
    var set = new mtgen.ViewModels.Set()
    {
        Code = "dka",
        Name = "Dark Ascension",
        CreatedDate = new DateTime(2012, 1, 25),
        CardCount = 158,
        StartProductName = "dka-prerelease"
    };

    set.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2014, 9, 19),
            HtmlString = "Upgraded to newest version, added all promos, Prerelease pack-ins, and the Fat Pack."
        },
        new Update() {
            UpdateDate = new DateTime(2014, 1, 25),
            HtmlString = "Added the proper Dark Ascension checklist. Thanks Jay!"
        },
        new Update() {
            UpdateDate = new DateTime(2014, 1, 25),
            HtmlString = "Added in the 3 new tokens: Human, Vampire, and Sorin. Are these added to the old Innistrad tokens or do they replace any? No idea. I just added them."
        }
    };

    set.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json"
										,"../isd/cardsMain.json", "../isd/cardsToken.json", "../isd/cardsOther.json" };
    set.PackFiles = new List<string>() { "packs.json", "../isd/packs.json" };

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}