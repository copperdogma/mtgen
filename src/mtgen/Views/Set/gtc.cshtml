@using mtgen.ViewModels;
@{
    var set = new mtgen.ViewModels.Set()
    {
        Code = "gtc",
        Name = "Gatecrash",
        CreatedDate = new DateTime(2013, 1, 1),
        CardCount = 249,
        StartProductName = "gtc-prerelease"
    };

    set.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2014,8,30),
            HtmlString = "Upgraded to newest version, added all Prerelease pack-ins, Fat Packs, and guild sorting."
        }
    };

    set.CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json", "../rtr/cardsOther.json" };

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}