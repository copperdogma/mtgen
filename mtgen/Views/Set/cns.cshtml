@using mtgen.Models;
@{
    var set = new mtgen.Models.Set()
    {
        Code = "cns",
        Name = "Conspiracy",
        CreatedDate = new DateTime(2014, 5, 29),
        CardCount = 210,
        StartProductName = "cns-boosters"
    };

    set.Updates = new List<Update>()
    {
        new Update() {
            UpdateDate = new DateTime(2014,8,23),
            HtmlString = "Upgraded to the new generator version."
        },
        new Update() {
            UpdateDate = new DateTime(2014, 5, 29),
            HtmlString = "Morning: The land slot now replaced with a Conspiracy on Construct card (50/50 chance), and that's the only place you get get one. Thanks, boocat!"
        }
    };

    ViewBag.Set = set;

    /* Defaults:
        SetFile = "~/sets.json";
        CardFiles = new List<string>() { "cardsMain.json", "cardsToken.json", "cardsOther.json" };
        PackFiles = new List<string>() { "packs.json" };
        ProductFile = "products.json";
     */
}